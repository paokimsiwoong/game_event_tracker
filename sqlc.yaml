version: "2"
sql:
  - schema: "sql/schema"
    queries: "sql/queries"
    engine: "postgresql"
    gen:
      go:
        out: "internal/database"
        sql_package: "pgx/v5" # Specifies pgx/v5 as the SQL package

# sqlc 문서
# https://docs.sqlc.dev/en/latest/tutorials/getting-started-postgresql.html